---
description: Представление типов времени работы Windows на JavaScript.
title: Представление типов среды выполнения Windows в JavaScript
ms.prod: microsoft-edge
ms.technology: windows-integration
ms.topic: article
helpviewer_keywords:
- Windows Runtime types [JavaScript]
- JavaScript, Windows Runtime types
ms.assetid: f4851802-8b40-4afa-ba6c-8a162a9a43cc
caps.latest.revision: 9
author: MSEdgeTeam
ms.author: msedgedevrel
ms.date: 11/19/2020
ROBOTS: NOINDEX,NOFOLLOW
ms.openlocfilehash: 1fd6c8831b2c98cea5794866a0f8830d8f557723
ms.sourcegitcommit: a35a6b5bbc21b7df61d08cbc6b074b5325ad4fef
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 12/17/2020
ms.locfileid: "11235847"
---
# <span data-ttu-id="54485-103">Представление типов среды выполнения Windows в JavaScript</span><span class="sxs-lookup"><span data-stu-id="54485-103">JavaScript representation of Windows Runtime types</span></span>  

[!INCLUDE [deprecation-note](../includes/legacy-edge-note.md)]  

<span data-ttu-id="54485-104">В следующей таблице описано, как JavaScript представляет типы времени работы Windows.</span><span class="sxs-lookup"><span data-stu-id="54485-104">The following table describes the way JavaScript represents Windows Runtime types.</span></span>  

> [!IMPORTANT]
> <span data-ttu-id="54485-105">Функции времени работы Windows недоступны для приложений, которые работают в Internet Explorer.</span><span class="sxs-lookup"><span data-stu-id="54485-105">Windows Runtime features are not available for apps that run in Internet Explorer.</span></span>  

## <span data-ttu-id="54485-106">Типы времени работы Windows в JavaScript</span><span class="sxs-lookup"><span data-stu-id="54485-106">Windows Runtime types in JavaScript</span></span>  

| <span data-ttu-id="54485-107">Тип времени работы Windows</span><span class="sxs-lookup"><span data-stu-id="54485-107">Windows Runtime type</span></span> | <span data-ttu-id="54485-108">Представление JavaScript</span><span class="sxs-lookup"><span data-stu-id="54485-108">JavaScript representation</span></span> | <span data-ttu-id="54485-109">Описание</span><span class="sxs-lookup"><span data-stu-id="54485-109">Description</span></span> |  
|:--- |:--- |:--- |  
| `UInt8` | `Number` | <span data-ttu-id="54485-110">Windows Runtime `Uint8` — это неподписаное 8-битное число, представляемые в качестве номера JavaScript.</span><span class="sxs-lookup"><span data-stu-id="54485-110">A Windows Runtime `Uint8` is an unsigned 8-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="54485-111">Значение JavaScript сначала преобразуется в число JavaScript, а затем процесс `ToUint32` следует за ним.</span><span class="sxs-lookup"><span data-stu-id="54485-111">A JavaScript value is first converted to a JavaScript number, and then the `ToUint32` process is followed.</span></span>  <span data-ttu-id="54485-112">Если значение номера JavaScript находится за пределами диапазона Uint8, к результату будет применено модуло 2^8.</span><span class="sxs-lookup"><span data-stu-id="54485-112">If the JavaScript number value is outside the range of a Uint8, the result will have modulo 2^8 applied.</span></span>  |  
| `Int32` | `Number` | <span data-ttu-id="54485-113">Время работы Windows `Int32` — это 32-битное 32-битное число, представляемые в качестве номера JavaScript.</span><span class="sxs-lookup"><span data-stu-id="54485-113">A Windows Runtime `Int32` is a signed 32-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="54485-114">Значение JavaScript сначала преобразуется в число JavaScript, а затем процесс `ToInt32` следует за ним.</span><span class="sxs-lookup"><span data-stu-id="54485-114">A JavaScript value is first converted to a JavaScript number, and then the `ToInt32` process is followed.</span></span>  <span data-ttu-id="54485-115">Если значение номера JavaScript находится за пределами диапазона, к результату будет применено модуло `Int32` 2^16.</span><span class="sxs-lookup"><span data-stu-id="54485-115">If the JavaScript number value is outside the range of an `Int32`, the result will have modulo 2^16 applied.</span></span>  |  
| `Int64` | `Number` | <span data-ttu-id="54485-116">Windows Runtime — это подписанное 64-битное число, представляемые как стандартное число, если оно попадает в диапазон `Int64` \[-2^53, 2^53\].</span><span class="sxs-lookup"><span data-stu-id="54485-116">A Windows Runtime `Int64` is a signed 64-bit integer, represented as a standard number if it falls within the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="54485-117">Если оно попадает за пределы этого диапазона, оно представляется как числовая величина с пользовательским хранилищем, которое поддерживает полные 64 бита полных данных.</span><span class="sxs-lookup"><span data-stu-id="54485-117">If it falls outside this range, it is represented as a number value that has a custom backing store that maintains the full 64 bits of integer data.</span></span>  <span data-ttu-id="54485-118">Все математические операции с этими пользовательскими числами Int64 приводят к преобразованию значения в стандартное число в диапазоне \[-2^53, 2^53\].</span><span class="sxs-lookup"><span data-stu-id="54485-118">All mathematical operations on these custom Int64 number values cause the value to be converted to a standard number in the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="54485-119">Если значение находится за пределами этого диапазона, вызывается ошибка типа.</span><span class="sxs-lookup"><span data-stu-id="54485-119">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="54485-120">Исключениями из этого процесса преобразования являются операции , , и, которые сравнивают аргументы на основе полных 64 бит, когда передаются два представленных `==` `===` `SameValue` `RelationalComparison` 64-битных значения.</span><span class="sxs-lookup"><span data-stu-id="54485-120">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two represented 64-bit values.</span></span>  <span data-ttu-id="54485-121">Если один из аргументов является стандартным номером JavaScript, эти операции также преобразуют его в число JavaScript перед сравнением.</span><span class="sxs-lookup"><span data-stu-id="54485-121">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="54485-122">Значение JavaScript напрямую назначено, если оно само является значением Int64; в противном случае результат применения преобразования к значению передается `ToInteger` в итоге в итоге.</span><span class="sxs-lookup"><span data-stu-id="54485-122">A JavaScript value is directly assigned if it is an Int64 value itself; otherwise the result of applying the `ToInteger` conversion on the value is passed to Windows Runtime.</span></span>  <span data-ttu-id="54485-123">Если не удается привести тип, возвращается исключение.</span><span class="sxs-lookup"><span data-stu-id="54485-123">If the type coercion fails, an exception is returned.</span></span>  |  
| `Uint64` | `Number` | <span data-ttu-id="54485-124">Windows Runtime — это неподписаное 64-битное число, представляемые как стандартное число, если оно попадает в диапазон `Uint64` \[0, 2^53\].</span><span class="sxs-lookup"><span data-stu-id="54485-124">A Windows Runtime `Uint64` is an unsigned 64-bit integer, represented as a standard number if it falls within the range \[0, 2^53\].</span></span>  <span data-ttu-id="54485-125">Если он попадает за пределы этого диапазона, он представлен как число с пользовательским хранилищем, которое поддерживает полные 64 бита неподписаных полных данных.</span><span class="sxs-lookup"><span data-stu-id="54485-125">If it falls outside this range, it is represented as a number that has a custom backing store that maintains the full 64 bits of unsigned integer data.</span></span>  <span data-ttu-id="54485-126">Все математические операции с этими пользовательскими значениями Uint64 приводят к преобразованию значения в стандартное число в диапазоне \[0, 2^53\].</span><span class="sxs-lookup"><span data-stu-id="54485-126">All mathematical operations on these custom Uint64 values cause the value to be converted to a standard number in the range \[0, 2^53\].</span></span>  <span data-ttu-id="54485-127">Если значение находится за пределами этого диапазона, вызывается ошибка типа.</span><span class="sxs-lookup"><span data-stu-id="54485-127">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="54485-128">Исключением из этого процесса преобразования являются операции , , и, которые сравнивают аргументы на основе полных 64 битов, когда передаются два `==` `===` `SameValue` `RelationalComparison` 64-битных значения.</span><span class="sxs-lookup"><span data-stu-id="54485-128">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two 64-bit values.</span></span>  <span data-ttu-id="54485-129">Если один из аргументов является стандартным номером JavaScript, эти операции также преобразуют его в число JavaScript перед сравнением.</span><span class="sxs-lookup"><span data-stu-id="54485-129">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="54485-130">Значение JavaScript напрямую назначено, если оно само является значением Uint64; в противном случае результат применения преобразования к значению с последующим принятием модуля 2^52 передается в порядок работы `ToInteger` Windows.</span><span class="sxs-lookup"><span data-stu-id="54485-130">A JavaScript value is directly assigned if it is a Uint64 value itself; otherwise, the result of applying the `ToInteger` conversion on the value, followed by taking modulo 2^52, is passed to Windows Runtime.</span></span>  <span data-ttu-id="54485-131">Если преобразование типа не удается, возвращается исключение.</span><span class="sxs-lookup"><span data-stu-id="54485-131">If the type conversion fails, an exception is returned.</span></span>  |  
| `Single` | `Number` | <span data-ttu-id="54485-132">Windows Runtime — это 32-разрядное число с плавающей за точкой, представленное в качестве числа JavaScript путем выбора ближайшего значения двойной точности к значению с одной `Single` точностью.</span><span class="sxs-lookup"><span data-stu-id="54485-132">A Windows Runtime `Single` is a 32-bit floating-point number, represented as a JavaScript number by picking the closest double-precision value to the single-precision value.</span></span>  <span data-ttu-id="54485-133">Значение JavaScript преобразуется в число JavaScript, а затем проверяется диапазон, чтобы убедиться, что значение может быть представлено в 32-разрядной 32-разрядной точке IEEE 754 с плавающей за точкой.</span><span class="sxs-lookup"><span data-stu-id="54485-133">A JavaScript value is converted to a JavaScript number and then range checked to ensure that the value can be represented in a single-precision 32-bit IEEE 754 floating-point number.</span></span>  <span data-ttu-id="54485-134">Если не удается проверить преобразование или диапазон, возвращается ошибка маршалирования.</span><span class="sxs-lookup"><span data-stu-id="54485-134">If the conversion or range check fails, a marshaling error is returned.</span></span>  |  
| `Double` | `Number` | <span data-ttu-id="54485-135">Windows Runtime `Double` — это 64-битное число с плавающей за точкой.</span><span class="sxs-lookup"><span data-stu-id="54485-135">A Windows Runtime `Double` is a 64-bit floating-point number.</span></span>  `ToNumber` <span data-ttu-id="54485-136">используется для преобразования времени работы Windows `Double` в число JavaScript.</span><span class="sxs-lookup"><span data-stu-id="54485-136">is used to convert a Windows Runtime `Double` to a JavaScript number.</span></span>  <span data-ttu-id="54485-137">Если преобразование не удается, возвращается ошибка маршалинга.</span><span class="sxs-lookup"><span data-stu-id="54485-137">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Boolean` | `Boolean` | <span data-ttu-id="54485-138">8-битное значение— это 8-битное значение, где ноль и любое другое значение, кроме нуля, представлено `Boolean` `false` как `true` `Boolean` JavaScript.</span><span class="sxs-lookup"><span data-stu-id="54485-138">A Windows Runtime `Boolean` is an 8-bit value where zero is `false` and any value other than zero is `true`, represented as a JavaScript `Boolean`.</span></span>  <span data-ttu-id="54485-139">Значение JavaScript сначала преобразуется в JavaScript `Boolean` с помощью `ToBoolean` .</span><span class="sxs-lookup"><span data-stu-id="54485-139">A JavaScript value is first converted to a JavaScript `Boolean` by `ToBoolean`.</span></span>  <span data-ttu-id="54485-140">Это означает, что функция времени работы Windows, объявленная как объявленная, может `void func(BOOL);` быть написана на JavaScript как `obj.func("test");` .</span><span class="sxs-lookup"><span data-stu-id="54485-140">This means that a Windows Runtime function declared as `void func(BOOL);` could be written in JavaScript as `obj.func("test");`.</span></span>  <span data-ttu-id="54485-141">Функция времени работы Windows вызвана с `BOOL` переданным параметром `TRUE` .</span><span class="sxs-lookup"><span data-stu-id="54485-141">The Windows Runtime function is called with the `BOOL` parameter passed as `TRUE`.</span></span>  <span data-ttu-id="54485-142">Если преобразование не удается, возвращается ошибка маршалинга.</span><span class="sxs-lookup"><span data-stu-id="54485-142">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Char16` | `String` | <span data-ttu-id="54485-143">Windows Runtime — это 16-разрядный символ Юникода, представленный в качестве строки `Char16` JavaScript, которая содержит один символ.</span><span class="sxs-lookup"><span data-stu-id="54485-143">A Windows Runtime `Char16` is a 16-bit Unicode character, represented as a JavaScript string that contains a single character.</span></span>  <span data-ttu-id="54485-144">Значение JavaScript преобразуется в строку JavaScript, а затем проверяется, чтобы строка была длиной всего `ToString` один символ.</span><span class="sxs-lookup"><span data-stu-id="54485-144">A JavaScript value is converted to a JavaScript string by `ToString` and then checked to ensure that the string is only a single character long.</span></span>  <span data-ttu-id="54485-145">Затем в качестве значения передается один `Char16` символ.</span><span class="sxs-lookup"><span data-stu-id="54485-145">The single character is then passed as the `Char16` value.</span></span>  <span data-ttu-id="54485-146">Если не удается проверить преобразование или длину, возвращается ошибка маршалирования.</span><span class="sxs-lookup"><span data-stu-id="54485-146">If the conversion or length check fails, a marshaling error is returned.</span></span>  |  
| `String` | `String` | <span data-ttu-id="54485-147">Windows Runtime `String` — это не изменяемая последовательность `Char16` объектов.</span><span class="sxs-lookup"><span data-stu-id="54485-147">A Windows Runtime `String` is an immutable sequence of `Char16` objects.</span></span>  <span data-ttu-id="54485-148">Строки представлены в качестве объектов `String` JavaScript.</span><span class="sxs-lookup"><span data-stu-id="54485-148">Strings are represented as JavaScript `String` objects.</span></span>  <span data-ttu-id="54485-149">Строки во время работы Windows не имеют разных значений для `null` пустой строки \(""\).</span><span class="sxs-lookup"><span data-stu-id="54485-149">Windows Runtime strings do not have different values for `null` and empty string \(""\).</span></span>  `null` <span data-ttu-id="54485-150">и пустые строки преобразуются в пустую строку JavaScript.</span><span class="sxs-lookup"><span data-stu-id="54485-150">and empty string values are converted to the JavaScript empty string.</span></span>  <span data-ttu-id="54485-151">**ПРИМЕЧАНИЕ.** Когда JavaScript передается параметру времени runtime Windows, который ожидает строку, он создает строку "null", а не пустую строку `null` `null` \(""\).</span><span class="sxs-lookup"><span data-stu-id="54485-151">**NOTE**:  When JavaScript `null` is passed to a Windows Runtime parameter that expects a string, it produces the string value "null", not `null` or the empty string \(""\).</span></span>  <span data-ttu-id="54485-152">Строки, переданные в пустую строку, всегда должны быть переданы в пустую строку.</span><span class="sxs-lookup"><span data-stu-id="54485-152">Strings passed to the Windows Runtime should always be instantiated to the empty string.</span></span>  |  
| `Enumeration` | `Object` | <span data-ttu-id="54485-153">`Enumeration`32-битное integer \(signed или unsigned\) — это 32-битное integer \(signed или unsigned\), со связанным набором именуемой констант.</span><span class="sxs-lookup"><span data-stu-id="54485-153">A Windows Runtime `Enumeration` is a 32-bit integer \(signed or unsigned\) that has an associated set of named constants.</span></span>  <span data-ttu-id="54485-154">В JavaScript enumeration is represented as an object that contains a read-only field for each named value.</span><span class="sxs-lookup"><span data-stu-id="54485-154">In JavaScript, an enumeration is represented as an object that contains a read-only field for each named value.</span></span>  <span data-ttu-id="54485-155">Значения enumeration преобразуются в числа JavaScript, как было определено ранее для `Int32` и `UInt32` .</span><span class="sxs-lookup"><span data-stu-id="54485-155">Enumeration values are converted to JavaScript numbers as defined earlier for `Int32` and `UInt32`.</span></span>  <span data-ttu-id="54485-156">Когда значение JavaScript преобразуется в индекс времени работы Windows, оно преобразуется, усечено и диапазон проверяется, как описано выше.</span><span class="sxs-lookup"><span data-stu-id="54485-156">When a JavaScript value is converted to a Windows Runtime enumeration, it is converted, truncated and range checked as described earlier.</span></span>  <span data-ttu-id="54485-157">Однако итоговые значения не проверяются с заданными именоваными значениями, указанными в этом значении.</span><span class="sxs-lookup"><span data-stu-id="54485-157">However, the resulting value is not checked against the defined named values that are specified in the enumeration.</span></span>  |  
| `Structure` | `Object` | <span data-ttu-id="54485-158">Windows Runtime `Structure` — это разнородная коллекция именованных полей данных.</span><span class="sxs-lookup"><span data-stu-id="54485-158">A Windows Runtime `Structure` is a heterogeneous collection of named data fields.</span></span>  <span data-ttu-id="54485-159">В JavaScript структура представлена как объект JavaScript, который содержит именуемого свойства для каждого поля в структуре.</span><span class="sxs-lookup"><span data-stu-id="54485-159">In JavaScript, a structure is represented as a JavaScript object that contains a named property for each field in the structure.</span></span>  <span data-ttu-id="54485-160">Если преобразование какого-либо значения структуры не удается, возвращается ошибка маршалирования.</span><span class="sxs-lookup"><span data-stu-id="54485-160">If the conversion of any structure value fails, a marshaling error is returned.</span></span>  <span data-ttu-id="54485-161">Поля в объекте JavaScript, которые не имеют эквивалентов в структуре времени работы Windows, игнорируются.</span><span class="sxs-lookup"><span data-stu-id="54485-161">Fields in the JavaScript object that do not have equivalents in the Windows Runtime structure are ignored.</span></span>  <span data-ttu-id="54485-162">**ПРИМЕЧАНИЕ.** Структуру времени работы Windows нельзя использовать в JavaScript с ключевым `new` словом.</span><span class="sxs-lookup"><span data-stu-id="54485-162">**NOTE**:  A Windows Runtime structure cannot be instantiated in JavaScript with the `new` keyword.</span></span>  |  
| `Array` | `Array` | <span data-ttu-id="54485-163">Время работы Windows `Array` преобразуется в массив JavaScript.</span><span class="sxs-lookup"><span data-stu-id="54485-163">A Windows Runtime `Array` is converted to a JavaScript array.</span></span>  <span data-ttu-id="54485-164">Однако массивы в среде разработки Windows не могут меняться, поэтому некоторые операции с массивами JavaScript недоступны.</span><span class="sxs-lookup"><span data-stu-id="54485-164">However, Windows Runtime arrays are not resizable, so some JavaScript array operations are not possible.</span></span>  <span data-ttu-id="54485-165">Внутреннее свойство преобразованного массива не имеет свойства "Array", так как это запрещено спецификацией `[[Class]]` ECMAScript 5.</span><span class="sxs-lookup"><span data-stu-id="54485-165">The internal `[[Class]]` property of a converted array is not set to "Array" because this is not allowed by the ECMAScript 5 specification.</span></span>  <span data-ttu-id="54485-166">Это означает, `Array.isArray(v)` что возвращает `false` .</span><span class="sxs-lookup"><span data-stu-id="54485-166">This means that `Array.isArray(v)` returns `false`.</span></span>  <span data-ttu-id="54485-167">Значение JavaScript преобразуется в массив времени runtime Windows следующим образом: если это значение или , оно преобразуется в `null` `undefined` родной `null` .</span><span class="sxs-lookup"><span data-stu-id="54485-167">A JavaScript value is converted to a Windows Runtime array as follows:  If the value is `null` or `undefined`, it is converted to a native `null`.</span></span>  <span data-ttu-id="54485-168">Если его `[[Class]]` внутреннее свойство — "Array", оно копируется в собственный массив, и передается ссылка на этот массив.</span><span class="sxs-lookup"><span data-stu-id="54485-168">If its internal `[[Class]]` property is "Array", it is copied to a native array and a reference to this array is passed.</span></span>  <span data-ttu-id="54485-169">Если это преобразованный массив, как описано выше, передается его в основном массиве native.</span><span class="sxs-lookup"><span data-stu-id="54485-169">If it is a converted array, as described previously, the underlying native array is passed.</span></span>  <span data-ttu-id="54485-170">**ПРИМЕЧАНИЕ.** Передача значения массива JavaScript методу времени работы Windows приводит к полному копированию массива.</span><span class="sxs-lookup"><span data-stu-id="54485-170">**NOTE**:  Passing a JavaScript array value to a Windows Runtime method causes a full copy of the array.</span></span>  |  
| <span data-ttu-id="54485-171">Delegate \(function callback\)</span><span class="sxs-lookup"><span data-stu-id="54485-171">Delegate \(function callback\)</span></span> | `Function` | <span data-ttu-id="54485-172">Делегат в времени работы Windows является ссылкой на один метод.</span><span class="sxs-lookup"><span data-stu-id="54485-172">A Windows Runtime delegate is a reference to a single method.</span></span>  <span data-ttu-id="54485-173">Делегат в среде запуска Windows представлен в JavaScript как JavaScript, который гарантированно будет вызван в `Function` соответствующем потоке.</span><span class="sxs-lookup"><span data-stu-id="54485-173">A Windows Runtime delegate is represented in JavaScript as a JavaScript `Function`, which is guaranteed to be called on the proper thread.</span></span>  <span data-ttu-id="54485-174">При вызове аргументы преобразуются в эквивалентные типы параметров `Function` времени работы Windows.</span><span class="sxs-lookup"><span data-stu-id="54485-174">When the `Function` is invoked, the arguments are converted to the equivalent Windows Runtime parameter types.</span></span>  <span data-ttu-id="54485-175">Если аргументов JavaScript меньше, чем параметров делегата, вызов `in` делегата не удастся.</span><span class="sxs-lookup"><span data-stu-id="54485-175">If there are fewer JavaScript arguments than delegate `in` parameters, the delegate call fails.</span></span>  <span data-ttu-id="54485-176">Если в делегате больше аргументов JavaScript, чем параметров, дополнительные аргументы `in` JavaScript игнорируются.</span><span class="sxs-lookup"><span data-stu-id="54485-176">If there are more JavaScript arguments than there are `in` parameters in the delegate, the extra JavaScript arguments are ignored.</span></span>  <span data-ttu-id="54485-177">После вызова делегата параметры преобразуются в типы `out` JavaScript и возвращаются.</span><span class="sxs-lookup"><span data-stu-id="54485-177">After the delegate is invoked, the `out` parameters are converted to JavaScript types and returned.</span></span>  <span data-ttu-id="54485-178">Если машинный объект функции JavaScript преобразуется в делегата времени работы Windows, он преобразуется в делегат времени запуска Windows соответствующего типа.</span><span class="sxs-lookup"><span data-stu-id="54485-178">If a native JavaScript function object is converted to a Windows Runtime delegate, it is wrapped in a Windows Runtime delegate of the corresponding type.</span></span>  <span data-ttu-id="54485-179">При вызове делегата `in` параметры преобразуются в типы JavaScript.</span><span class="sxs-lookup"><span data-stu-id="54485-179">When the delegate is invoked, the `in` parameters are converted to JavaScript types.</span></span>  <span data-ttu-id="54485-180">После вызова делегата возвращаемая величина преобразуется в `out` параметры делегата.</span><span class="sxs-lookup"><span data-stu-id="54485-180">After the delegate is invoked, the return value is converted to the `out` parameters of the delegate.</span></span>  |  
| <span data-ttu-id="54485-181">Интерфейс</span><span class="sxs-lookup"><span data-stu-id="54485-181">Interface</span></span> | `Object` | <span data-ttu-id="54485-182">Интерфейсы и группы интерфейсов не представлены непосредственно в JavaScript как объекты.</span><span class="sxs-lookup"><span data-stu-id="54485-182">Interfaces and interface groups are not directly represented in JavaScript as objects.</span></span>  <span data-ttu-id="54485-183">Однако интерфейсы представлены в качестве параметров и возвращаемого типа методов времени работы Windows.</span><span class="sxs-lookup"><span data-stu-id="54485-183">However, interfaces are represented as parameter and return types of Windows Runtime methods.</span></span>  <span data-ttu-id="54485-184">Экземпляр интерфейса времени работы Windows преобразуется следующим образом:</span><span class="sxs-lookup"><span data-stu-id="54485-184">A Windows Runtime interface instance is converted as follows:</span></span><br /> <br /> <span data-ttu-id="54485-185">1. Если существует допустимый класс времени работы, представить объект в качестве экземпляра этого класса.</span><span class="sxs-lookup"><span data-stu-id="54485-185">1.  If there is a valid runtime class, represent the object as an instance of that class.</span></span><br /> <span data-ttu-id="54485-186">2. Если допустимого класса времени выполнения нет, представить объект как экземпляр неименоваемого класса времени выполнения, который реализует в точности интерфейс, реализуемого экземпляром, а также его необходимые интерфейсы.</span><span class="sxs-lookup"><span data-stu-id="54485-186">2.  If there is no valid runtime class, represent the object as an instance of an unnamed runtime class that implements exactly the interface that the instance is known to implement plus its required interfaces.</span></span><br /> <br /> <span data-ttu-id="54485-187">Значение JavaScript тестировано, чтобы определить, является ли это экземпляром класса времени работы или интерфейсом.</span><span class="sxs-lookup"><span data-stu-id="54485-187">A JavaScript value is tested to determine whether it is an instance of a runtime class or an interface.</span></span>  <span data-ttu-id="54485-188">Если это так, а исходное значение времени выполнения Windows реализует интерфейс, объект передается в начальную начальную окна windows.</span><span class="sxs-lookup"><span data-stu-id="54485-188">If it is, and the original Windows Runtime value implements the interface, the object is passed to the Windows Runtime.</span></span>  |  
| <span data-ttu-id="54485-189">Классы времени работы</span><span class="sxs-lookup"><span data-stu-id="54485-189">Runtime classes</span></span> | `Object` | <span data-ttu-id="54485-190">Объектами в времени выполнения Windows могут быть экземпляры классов времени выполнения, которые реализуют один или несколько интерфейсов.</span><span class="sxs-lookup"><span data-stu-id="54485-190">Objects in the Windows Runtime can be instances of runtime classes that implement one or more interfaces.</span></span>  <span data-ttu-id="54485-191">Объекты времени запуска Windows представлены в JavaScript как объекты.</span><span class="sxs-lookup"><span data-stu-id="54485-191">Windows Runtime objects are represented in JavaScript as objects.</span></span>  <span data-ttu-id="54485-192">Объединение методов, свойств и событий, определенных во всех реализованных интерфейсах класса, представляет именуемые свойства в прототипе объекта JavaScript.</span><span class="sxs-lookup"><span data-stu-id="54485-192">The union of methods, properties, and events defined on all the implemented interfaces of the class represents the named properties in the prototype of the JavaScript object.</span></span>  <span data-ttu-id="54485-193">Пользователи JavaScript могут получить доступ к любому члену класса напрямую.</span><span class="sxs-lookup"><span data-stu-id="54485-193">JavaScript consumers can access any member of the class directly.</span></span>  <span data-ttu-id="54485-194">Объекты времени выполнения Windows имеют прототип, содержащий все члены из всех реализованных интерфейсов класса.</span><span class="sxs-lookup"><span data-stu-id="54485-194">Windows Runtime objects have a prototype that contains all the members from all the implemented interfaces of the runtime class.</span></span>  |  
  
## <span data-ttu-id="54485-195">См. также</span><span class="sxs-lookup"><span data-stu-id="54485-195">See also</span></span>  

[<span data-ttu-id="54485-196">Использование среды выполнения Windows в JavaScript</span><span class="sxs-lookup"><span data-stu-id="54485-196">Using the Windows Runtime in JavaScript</span></span>][WindowsRuntimeJavascript]  

<!-- links -->  

[WindowsRuntimeJavascript]: ./using-the-windows-runtime-in-javascript.md "Использование точки запуска Windows в JavaScript | Документы Майкрософт"  
